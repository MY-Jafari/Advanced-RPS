[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "elemental-rps"
version = "1.0.0"
description = "Advanced 6-element Rock-Paper-Scissors game with Pygame"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pygame>=2.0.0"
]
